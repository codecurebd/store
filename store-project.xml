<project name="CodeCureBD" version="1.0">

  <!-- ================================================= -->
  <!-- 1. Landing Page (index.html)                       -->
  <!-- ================================================= -->
  <file path="index.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes" />
  <title> CodeCureBD </title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:opsz@14..32&display=swap" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="stylesheet" href="style.css" />
  <style>
    * { box-sizing: border-box; }
    body { background: #f2f4f7; font-family: 'Inter', sans-serif; margin: 0; padding: 0; min-height: 100vh; }
    .fade-up { opacity: 0; transform: translateY(30px); transition: opacity 0.6s ease, transform 0.6s ease; }
    .fade-up.visible { opacity: 1; transform: translateY(0); }
    .scroll-top-btn { position: fixed; bottom: 30px; right: 30px; width: 50px; height: 50px; border-radius: 50%; background: linear-gradient(135deg, #007AFF, #5856D6); color: #fff; border: none; font-size: 1.2rem; cursor: pointer; z-index: 999; opacity: 0; transition: 0.3s; pointer-events: none; }
    .scroll-top-btn.show { opacity: 1; pointer-events: auto; }
    .glass-card { background: rgba(255,255,255,0.72); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.7); border-radius: 16px; padding: 1.5rem; }
    .btn-primary { background: linear-gradient(135deg, #007AFF, #5856D6); color: #fff; padding: 12px 28px; border-radius: 60px; font-weight: 600; border: none; cursor: pointer; display: inline-flex; align-items: center; gap: 10px; text-decoration: none; }
    .btn-outline { background: rgba(255,255,255,0.5); color: #1c1c1e; padding: 12px 28px; border-radius: 60px; font-weight: 600; border: 1.5px solid rgba(60,60,67,0.08); cursor: pointer; display: inline-flex; align-items: center; gap: 10px; text-decoration: none; }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <div id="navbar-placeholder"></div>
    <main class="main-content">
      <section class="pt-32 pb-16 px-6 text-center">
        <h1 class="text-4xl md:text-6xl font-bold text-gray-900 fade-up">Build Your Dream Website Today</h1>
        <p class="text-lg text-gray-500 mt-4 fade-up">Professional website development, fixing, and maintenance.</p>
        <div class="mt-8 flex justify-center gap-4 fade-up">
          <a href="get-new-website.html" class="btn-primary"><i class="fas fa-store"></i> Explore Store</a>
          <a href="messages.html" class="btn-outline"><i class="fas fa-envelope"></i> Contact Us</a>
        </div>
      </section>
    </main>
    <div id="footer-placeholder"></div>
  </div>
  <button class="scroll-top-btn" id="scrollTopBtn"><i class="fas fa-arrow-up"></i></button>
  <script type="module">
    import { renderNavbar, renderFooter, updateCartBadge, renderCartSidebar } from './components.js';
    renderNavbar(); renderFooter(); renderCartSidebar(); updateCartBadge();
    window.addEventListener('scroll', () => {
      document.getElementById('scrollTopBtn').classList.toggle('show', window.scrollY > 400);
    });
    document.getElementById('scrollTopBtn').addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
  </script>
</body>
</html>
  </file>

  <!-- ================================================= -->
  <!-- 2. Products / Store Page (get-new-website.html)    -->
  <!-- ================================================= -->
  <file path="get-new-website.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Get New Website – CodeCureBD</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="stylesheet" href="style.css" />
  <style>
    * { box-sizing: border-box; }
    body { background: #f2f4f7; font-family: 'Inter', sans-serif; margin: 0; padding: 0; min-height: 100vh; }
    .glass-card { background: rgba(255,255,255,0.72); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.7); border-radius: 16px; padding: 1.25rem; }
    .product-card img { width: 100%; height: 200px; object-fit: cover; border-radius: 12px 12px 0 0; }
    .form-input { width: 100%; padding: 12px 16px; border: 1.5px solid rgba(60,60,67,0.08); border-radius: 12px; background: #fff; outline: none; }
    .btn-primary { background: linear-gradient(135deg, #007AFF, #5856D6); color: #fff; padding: 10px 22px; border-radius: 60px; font-weight: 600; border: none; cursor: pointer; display: inline-flex; align-items: center; gap: 8px; }
    .modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.4); backdrop-filter: blur(6px); z-index: 300; display: none; align-items: center; justify-content: center; padding: 1rem; }
    .modal-overlay.open { display: flex; }
    .modal-box { background: white; border-radius: 24px; padding: 1.5rem; max-width: 700px; width: 100%; max-height: 90vh; overflow-y: auto; }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <div id="navbar-placeholder"></div>
    <main class="main-content">
      <section class="pt-24 pb-12 px-6">
        <div class="max-w-7xl mx-auto">
          <h1 class="text-3xl font-bold text-gray-900">Choose Your Website Package</h1>
          <div class="flex flex-wrap gap-4 mt-4 items-center">
            <div class="relative flex-1 min-w-[200px]">
              <i class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
              <input type="text" id="searchInput" placeholder="Search packages..." class="form-input pl-10" />
            </div>
            <select id="categoryFilter" class="form-input max-w-xs">
              <option value="all">All Categories</option>
              <option value="static">Static</option>
              <option value="spa">SPA</option>
              <option value="dynamic">Dynamic</option>
              <option value="ecommerce">E-Commerce</option>
            </select>
            <select id="sortFilter" class="form-input max-w-xs">
              <option value="price-asc">Price: Low to High</option>
              <option value="price-desc">Price: High to Low</option>
              <option value="name">Name: A to Z</option>
            </select>
          </div>
          <div id="productGrid" class="grid md:grid-cols-3 gap-6 mt-8">
            <!-- Dynamic Products Loaded via JS -->
          </div>
        </div>
      </section>
    </main>
    <div id="footer-placeholder"></div>
  </div>

  <script type="module">
    import { auth, db, collection, getDocs, doc, getDoc, onSnapshot } from './firebase-config.js';
    import { renderNavbar, renderFooter, updateCartBadge, renderCartSidebar } from './components.js';

    renderNavbar(); renderFooter(); renderCartSidebar(); updateCartBadge();

    let allProducts = [];
    const grid = document.getElementById('productGrid');
    const searchInput = document.getElementById('searchInput');

    onSnapshot(collection(db, 'products'), (snapshot) => {
      allProducts = [];
      snapshot.forEach(doc => allProducts.push({ id: doc.id, ...doc.data() }));
      renderProducts();
    });

    function renderProducts() {
      grid.innerHTML = allProducts.map(p => `
        <div class="glass-card product-card">
          <img src="${p.imageUrl || 'https://via.placeholder.com/300x200'}" alt="${p.name}" />
          <div class="p-4">
            <h3 class="font-bold text-xl">${p.name}</h3>
            <p class="text-sm text-gray-500 mt-1">${p.desc || ''}</p>
            <p class="text-2xl font-bold text-blue-600 mt-2">$${p.price}</p>
            <button onclick="window.addToCart('${p.id}')" class="btn-primary mt-3 text-sm py-2 px-4 w-full justify-center">
              <i class="fas fa-cart-plus"></i> Add to Cart
            </button>
          </div>
        </div>
      `).join('');
    }

    window.addToCart = (productId) => {
      const product = allProducts.find(p => p.id === productId);
      if (!product) return;
      const cart = JSON.parse(localStorage.getItem('cart')) || [];
      const existing = cart.find(item => item.id === productId);
      if (existing) existing.quantity += 1;
      else cart.push({ id: product.id, name: product.name, price: product.price, quantity: 1 });
      localStorage.setItem('cart', JSON.stringify(cart));
      updateCartBadge();
      alert(`Added "${product.name}" to cart!`);
    };

    searchInput.addEventListener('input', renderProducts);
  </script>
</body>
</html>
  </file>

  <!-- ================================================= -->
  <!-- 3. Contact/Messages Page (messages.html)           -->
  <!-- ================================================= -->
  <file path="messages.html">
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Contact Us – CodeCureBD</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="stylesheet" href="style.css" />
  <style>
    * { box-sizing: border-box; }
    body { background: #f2f4f7; font-family: 'Inter', sans-serif; margin: 0; padding: 0; min-height: 100vh; }
    .glass-card { background: rgba(255,255,255,0.72); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.7); border-radius: 16px; padding: 2rem; }
    .form-input { width: 100%; padding: 12px 16px; border: 1.5px solid rgba(60,60,67,0.08); border-radius: 12px; background: rgba(255,255,255,0.8); outline: none; transition: 0.3s; }
    .form-input:focus { border-color: #007AFF; background: #fff; box-shadow: 0 0 0 4px rgba(0, 122, 255, 0.08); }
    .btn-primary { background: linear-gradient(135deg, #007AFF, #5856D6); color: #fff; padding: 12px 28px; border-radius: 60px; font-weight: 600; border: none; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; gap: 8px; width: 100%; }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <div id="navbar-placeholder"></div>
    <main class="main-content">
      <section class="pt-32 pb-16 px-6 max-w-4xl mx-auto">
        <div class="text-center mb-10">
          <h1 class="text-4xl font-bold text-gray-900">Get in Touch</h1>
          <p class="text-gray-500 mt-2">Have questions or need a custom solution? Send us a message.</p>
        </div>
        <div class="grid md:grid-cols-2 gap-8">
          <div class="glass-card">
            <h3 class="text-xl font-bold text-gray-900 mb-4">Contact Information</h3>
            <p class="text-gray-600 mb-4">Feel free to reach out to us through any of the channels below.</p>
            <div class="space-y-4 text-gray-700">
              <div class="flex items-center gap-3"><i class="fas fa-envelope text-blue-600"></i> nopqrshov337@gmail.com</div>
              <div class="flex items-center gap-3"><i class="fas fa-phone text-blue-600"></i> +880 1350-141762</div>
              <div class="flex items-center gap-3"><i class="fas fa-map-marker-alt text-blue-600"></i> Dhaka, Bangladesh</div>
            </div>
          </div>
          <div class="glass-card">
            <form id="contactForm" class="space-y-4">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Your Name</label>
                <input type="text" id="contactName" placeholder="John Doe" required class="form-input" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
                <input type="email" id="contactEmail" placeholder="john@example.com" required class="form-input" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Message</label>
                <textarea id="contactMessage" rows="4" placeholder="How can we help you?" required class="form-input"></textarea>
              </div>
              <button type="submit" class="btn-primary"><i class="fas fa-paper-plane"></i> Send Message</button>
            </form>
          </div>
        </div>
      </section>
    </main>
    <div id="footer-placeholder"></div>
  </div>

  <script type="module">
    import { db, collection, addDoc, serverTimestamp } from './firebase-config.js';
    import { renderNavbar, renderFooter, updateCartBadge, renderCartSidebar } from './components.js';

    renderNavbar(); renderFooter(); renderCartSidebar(); updateCartBadge();

    const contactForm = document.getElementById('contactForm');
    contactForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      const name = document.getElementById('contactName').value.trim();
      const email = document.getElementById('contactEmail').value.trim();
      const message = document.getElementById('contactMessage').value.trim();

      try {
        await addDoc(collection(db, 'contacts'), {
          name, email, message, createdAt: serverTimestamp()
        });
        alert('✅ Message sent successfully! We will get back to you soon.');
        contactForm.reset();
      } catch (err) {
        alert('⚠️ Error sending message: ' + err.message);
      }
    });
  </script>
</body>
</html>
  </file>

</project>